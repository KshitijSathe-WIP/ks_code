# demo_patch.yaml
# ─────────────────────────────────────────────────────────────
# Demo: Update the lookup condition on the DTI→AGG_ACCOUNT edge
# to add a validity check alongside the key match.
# ─────────────────────────────────────────────────────────────
changes:
  - operation: UPDATE
    entity: cosmos_edge
    edge_id: "CRDM_DDM.F_ACCOUNTS.DTI_KEY__to__CRDM_DDM.AGG_ACCOUNT.H1_RISK_DTI_RANGE_1_CD__m_DDM_to_AGG_ACCOUNT"
    property: lookup_condition
    new_value: "DTI_KEY = i_DTI_KEY"

  - operation: UPDATE
    entity: cosmos_edge
    edge_id: "CRDM_DDM.F_ACCOUNTS.DTI_KEY__to__CRDM_DDM.AGG_ACCOUNT.H2_RISK_DTI_RANGE_2_DESC__m_DDM_to_AGG_ACCOUNT"
    property: lookup_condition
    new_value: "DTI_KEY = i_DTI_KEY"